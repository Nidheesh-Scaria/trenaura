<head>

<style>
  /* Main Content Container */
.main-content {
  flex: 1;
  padding: 40px;
  display: flex;
  flex-direction: column;
  background: linear-gradient(135deg, #f5f7fa 0%, #e8ecf1 100%);
  min-height: 100vh;
  animation: fadeIn 0.8s ease forwards;
}

@keyframes fadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

/* Page Header */
.main-content h2 {
  font-size: 32px;
  font-weight: 700;
  background: #233c79;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  display: flex;
  align-items: center;
  gap: 12px;
  margin: 0 0 8px 0;
  animation: slideDown 0.6s ease forwards;
  will-change: transform, opacity;
}

.main-content h2::before {
  content: '🔐';
  -webkit-text-fill-color: initial;
  font-size: 36px;
  transform-origin: center;
  animation: bounceIn 0.8s ease forwards;
}

@keyframes slideDown {
  from {
    opacity: 0;
    transform: translateY(-25px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes bounceIn {
  0%, 20%, 50%, 80%, 100% {
    transform: translateY(0);
  }
  40% {
    transform: translateY(-15px);
  }
  60% {
    transform: translateY(-7px);
  }
}

.main-content hr {
  border: none;
  height: 3px;
  background: linear-gradient(90deg, #233c79, #233c79, transparent);
  margin: 16px 0 32px 0;
  border-radius: 2px;
  animation: expandWidth 0.9s ease forwards;
}

@keyframes expandWidth {
  from {
    width: 0;
  }
  to {
    width: 100%;
  }
}

/* Password Box Container */
.password-box {
  background: #ffffff;
  padding: 40px 45px;
  border-radius: 20px;
  max-width: 600px;
  width: 100%;
  box-shadow: 0 12px 48px rgba(0, 0, 0, 0.07);
  animation: fadeInUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
  position: relative;
  overflow: hidden;
}

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(35px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* Decorative top border */
.password-box::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 5px;
  background-size: 200% 100%;
  animation: gradientMove 6s ease infinite;
}

@keyframes gradientMove {
  0%, 100% {
    background-position: 0% 50%;
  }
  50% {
    background-position: 100% 50%;
  }
}

/* Form Styling */
#changePasswordForm {
  display: flex;
  flex-direction: column;
  gap: 24px;
}

/* Form Groups */
.password-box .form-group {
  display: flex;
  flex-direction: column;
  gap: 10px;
  position: relative;
  margin-bottom: 0;
}

.password-box label {
  font-size: 14px;
  font-weight: 700;
  color: #1a1a1a;
  letter-spacing: 0.3px;
  display: flex;
  align-items: center;
  gap: 8px;
  margin-bottom: 0;
}

.password-box label::before {
  content: '•';
  color: #233c79;
  font-size: 20px;
  font-weight: bold;
  display: inline-block;
  line-height: 14px;
  transform: translateY(-1px);
}

/* Input Fields */
.password-box input {
  width: 100%;
  padding: 16px 20px;
  font-size: 15px;
  border: 2px solid #e0e6ed;
  border-radius: 12px;
  background-color: #fafbfd;
  transition: border-color 0.35s cubic-bezier(0.4, 0, 0.2, 1), background-color 0.35s;
  font-family: inherit;
  color: #333;
  box-sizing: border-box;
}

.password-box input::placeholder {
  color: #9ca3af;
  font-weight: 400;
}

.password-box input:hover {
  border-color: #a0aec0;
  background-color: #fff;
}

.password-box input:focus {
  border-color: #233c79;
  outline: none;
  background-color: #fff;
  box-shadow: 0 0 0 5px rgba(35, 60, 121, 0.15);
  transform: translateY(-1px);
  transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
}

/* Error Message */
.error-message {
  color: #ef4444;
  font-size: 14px;
  font-weight: 600;
  padding: 14px 18px;
  background: linear-gradient(135deg, #fee2e2, #fecaca);
  border-radius: 10px;
  margin: 0;
  display: none;
  animation: shake 0.4s ease;
}

.error-message:not(:empty) {
  display: block;
}

@keyframes shake {
  0%, 100% {
    transform: translateX(0);
  }
  25%, 75% {
    transform: translateX(-6px);
  }
  50% {
    transform: translateX(6px);
  }
}

.error-message::before {
  content: '⚠️ ';
  margin-right: 8px;
}

/* Form Actions */
.form-actions {
  display: flex;
  gap: 14px;
  margin-top: 12px;
}

.form-actions button {
  flex: 1;
  padding: 16px 24px;
  font-size: 15px;
  font-weight: 700;
  border: none;
  border-radius: 12px;
  cursor: pointer;
  overflow: hidden;
  text-transform: uppercase;
  letter-spacing: 0.8px;
  position: relative;
  background-clip: padding-box;
  transition: background-color 0.25s ease, box-shadow 0.25s ease, transform 0.15s ease;
}

.form-actions button::before {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  width: 0;
  height: 0;
  border-radius: 50%;
  transform: translate(-50%, -50%);
  transition: width 0.45s ease, height 0.45s ease;
  background: rgba(255, 255, 255, 0.3);
  pointer-events: none;
}

/* Save Button */
.save-btn {
  background: linear-gradient(135deg, #233c79 0%, #1a2c58 100%);
  color: white;
  box-shadow: 0 5px 18px rgba(35, 60, 121, 0.35);
}

.save-btn:hover {
  background: linear-gradient(135deg, #1a2c58 0%, #0f1a35 100%);
  box-shadow: 0 7px 28px rgba(35, 60, 121, 0.5);
  transform: translateY(-2px);
}

.save-btn:active {
  transform: translateY(0);
}

/* Cancel Button */
.cancel-btn {
  background: #ffffff;
  color: #374151;
  border: 2px solid #e5e7eb;
  box-shadow: 0 1px 4px rgba(0,0,0,0.05);
}

.cancel-btn:hover {
  background: #f9fafb;
  color: #ef4444;
  border-color: #ef4444;
  transform: translateY(-2px);
  box-shadow: 0 6px 18px rgba(239, 68, 68, 0.25);
}

.cancel-btn:active {
  transform: translateY(0);
}

/* Forgot Password Link */
.forgot-password-link {
  margin-top: 28px;
  text-align: center;
  padding-top: 24px;
  border-top: 2px dashed #e5e7eb;
}

.forgot-password-link a {
  color: #233c79;
  font-size: 15px;
  font-weight: 600;
  text-decoration: none;
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 10px 20px;
  border-radius: 8px;
  transition: all 0.3s ease;
  background: #f5f7fb;
}

.forgot-password-link a::before {
  content: '🔑';
  font-size: 18px;
  transition: transform 0.3s ease;
}

.forgot-password-link a:hover {
  background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
  color: #fff;
  box-shadow: 0 5px 16px rgba(239, 68, 68, 0.3);
  transform: translateY(-2px);
  text-decoration: none;
}

.forgot-password-link a:hover::before {
  transform: rotate(-15deg);
}

/* Loading State */
.form-actions button.loading {
  pointer-events: none;
  opacity: 0.75;
  position: relative;
}

.form-actions button.loading::after {
  content: '';
  position: absolute;
  width: 20px;
  height: 20px;
  top: 50%;
  left: 50%;
  margin-left: -10px;
  margin-top: -10px;
  border: 3.5px solid #ffffff;
  border-radius: 50%;
  border-top-color: transparent;
  animation: spinner 0.7s linear infinite;
}

@keyframes spinner {
  to {
    transform: rotate(360deg);
  }
}

/* Focus Visible for Accessibility */
button:focus-visible,
input:focus-visible,
a:focus-visible {
  outline: 3px solid #233c79;
  outline-offset: 2px;
}

/* Responsive styles remain unchanged */

</style>

</head>

<div class="container">
 {{> user/sidebar }} 

  <div class="main-content">
    <h2>Change password</h2>
    <hr style="margin: 10px 0 20px;">

    <div class="password-box">
      <form action="/handleChangePassword" method="POST" id="changePasswordForm"> 
        <div class="form-group">
          <label for="currentPassword">Current password</label>
          <input type="password" id="currentPassword" name="currentPassword" placeholder="Enter current password" >
        </div>

        <div class="form-group">
          <label for="newPassword">New password</label>
          <input type="password" id="newPassword" name="newPassword" placeholder="Enter new password" >
        </div>

        <div class="form-group">
          <label for="confirmPassword">Confirm password</label>
          <input type="text" id="confirmPassword" name="confirmPassword" placeholder="confirm new password" >
        </div>
        <div>
          
            <p class="error-message" id="errorMessage"></p>
        
        </div>

        <div class="form-actions">
          <button type="submit" class="save-btn">Save</button>
          <button type="button" class="cancel-btn" onclick="window.location.href='/myAccount'">Cancel</button>
        </div>
      </form>
      <div class="forgot-password-link">
          <a href="/forgotPassword-changePswrd">Forgot Password?</a>
      </div>
    </div>
  </div>
</div>

<script src="js/changePassword.js"></script>
<script type="text/javascript" src="https://cdn.jsdelivr.net/npm/toastify-js"></script>

