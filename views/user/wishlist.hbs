

<style>
  .wishlist-container {
    padding: 40px 10%;
    background-color: #fff;
  }

  .wishlist-heading {
    text-align: center;
    font-size: 24px;
    font-weight: bold;
    margin-bottom: 5px;
  }

  .wishlist-subtext {
    text-align: center;
    font-size: 12px;
    color: #888;
    margin-bottom: 30px;
  }

  .wishlist-item {
    display: flex;
    gap: 20px;
    background-color: #f5f5f5;
    border-radius: 5px;
    padding: 15px;
    margin-bottom: 20px;
    position: relative;
  }

  .wishlist-item img {
    width: 100px;
    height: auto;
    object-fit: contain;
  }

  .wishlist-details {
    flex-grow: 1;
  }

  .wishlist-details h3 {
    font-size: 16px;
    font-weight: bold;
    color: #111;
    margin-bottom: 8px;
  }

  .wishlist-price {
    font-size: 16px;
    font-weight: 600;
    color: #000;
  }

  .wishlist-price del {
    font-size: 14px;
    color: #999;
    margin-left: 10px;
  }

  .wishlist-delete {
    position: absolute;
    top: 10px;
    right: 10px;
    color: #888;
    cursor: pointer;
    transition: color 0.2s ease;
  }

  .wishlist-delete:hover {
    color: red;
  }


  .wishlist-empty-container {
  text-align: center;
  padding: 40px 20px;
  color: #555;
  animation: fadeIn 0.5s ease-in-out;
}

.wishlist-empty-icon {
  font-size: 60px;
  margin-bottom: 10px;
}

.wishlist-empty-title {
  margin: 0 0 10px;
  font-weight: bold;
  font-size: 24px;
}

.wishlist-empty-text {
  margin: 0 0 20px;
  font-size: 16px;
  color: #777;
}

.wishlist-empty-button {
  display: inline-block;
  padding: 10px 20px;
  background: linear-gradient(to right, #2193b0, #6dd5ed);
  color: white;
  text-decoration: none;
  border-radius: 25px;
  font-weight: bold;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
  transition: background 0.3s ease;
}

.wishlist-empty-button:hover {
  
   background: linear-gradient(to right, #f12711, #f5af19);
   
}

@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(15px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
.stylish-delete-btn {
  background: linear-gradient(to right, #ff0000, #ff4b2b); /* Red-pink gradient */
  border: none;
  border-radius: 50%;
  color: white;
  font-size: 1.1rem;
  padding: 10px;
  cursor: pointer;
  transition: all 0.3s ease;
  box-shadow: 0 4px 8px rgba(0,0,0,0.2);
}

.stylish-delete-btn:hover {
  transform: scale(1.1);
  color: white;
  background: linear-gradient(to right, #e60023, #ff4b2b);
  box-shadow: 0 6px 12px rgba(0,0,0,0.3);
}



</style>
<div class="wishlist-container">
  <div class="wishlist-heading">WISHLIST</div>
  <div class="wishlist-subtext">ONE CLICK CLOSER TO YOUR WISH</div>

  {{#if isWishListEmpty}}
    <div class="wishlist-empty-container">
      <div class="wishlist-empty-icon">💔</div>
      <h2 class="wishlist-empty-title">Your Wishlist is Empty</h2>
      <p class="wishlist-empty-text">Looks like you haven't added anything yet.</p>
      <a href="/products" class="wishlist-empty-button">Start Shopping</a>
    </div>
  {{/if}}

    {{#each wishlistItems}}
    <div class="wishlist-item" id="wishlist-item-{{this.productsId._id}}">
      <img src="/images/{{this.productsId.productImages.[0]}}" alt="{{this.productsId.productName}}">
      <div class="wishlist-details">
        <h3>{{this.productsId.productName}}</h3>
        <p class="product-description">{{this.productsId.description}}</p>
        <div class="wishlist-price">
          ₹{{this.productsId.salePrice}} 
          {{#if this.productsId.regularPrice}}<del>₹{{this.productsId.regularPrice}}</del>{{/if}}
        </div>
      </div>
      <form onclick="removeFromWishlist(event,'{{this.productsId._id}}');" >
       <button type="submit" class="wishlist-delete stylish-delete-btn">
        <i class="fa-solid fa-trash-can"></i>
      </button>
      </form>
    </div>
    {{/each}}
 
    


</div>


<script>
  async function removeFromWishlist(event,id){
    event.preventDefault();


      const res = await fetch(`/removeFromWishlist/${id}`, {
                    method: "DELETE",
                    headers: {
                      "Content-Type": "application/json"
                    }
                  });

    const data=await res.json();

    if(res.ok){
      
    Toastify({
        text: data.message,
        duration: 3000,
        close: true,          
        gravity: "top",       
        position: "center",    
        style: {
            background: "linear-gradient(to right, #0f2027, #203a43, #2c5364)", 
        },
        className: "custom-toast", 
        onClick: () => {     
            alert("Toast clicked!");
        }
        }).showToast();
        document.getElementById(`wishlist-item-${id}`)?.remove();
    }else{
      console.log("error deleting wishlist")
    }

  }
</script>

